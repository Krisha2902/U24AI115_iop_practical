#include<stdio.h>

int main()
{
    int base,power;
    float result=1;
    printf("enter base:\n");
    scanf("%d",&base);
    printf("enter power:\n",power);
    scanf("%d",&power);
    for(int i=1;i<=power;i++);
    {
    result*=base=;
    }
    printf("%d ^%d is %f\n",base,power,reuslt);
    return 0;
}