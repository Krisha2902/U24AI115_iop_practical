#include <stdio.h>

int main()
{
    int a,h,b;
    printf("the height of triangle is=");
    scanf("%d",&h);
    printf("the base of triangle is=");
    scanf("%d",&b);
    a=0.5*h*b;
    printf("the area of the truangle is=%d",a);

    return 0;
}